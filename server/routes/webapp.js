// serve / and /index(.html ?)
// serve static/... (css, img, js)

// serve /[documentid]/ ==> reader
// serve /[documentid]/edit ==> writer
// NB. documentid can contain "/": it is a normalization of document id
// (everything lowercase, removes spaces (and accents?))
